<?xml version="1.0" encoding="UTF-8" ?>
<class name="GDKGameSave" inherits="RefCounted" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		GDK-native file-style game save service accessed via GDK.game_save.
	</brief_description>
	<description>
		Wraps [code]XGameSaveFiles[/code] to resolve a user's cloud-synced save folder and query the remaining storage quota. This is the simpler file-based variation of the connected-storage API and is distinct from PlayFab Game Saves ([code]godot_playfab[/code]) and from [GDKTitleStorage]. These calls authenticate through Xbox services: the title needs its Partner Center [code]TitleId[/code] and [code]MSAAppId[/code] in [code]MicrosoftGame.config[/code], a SCID matching Partner Center, Connected Storage enabled under Gameplay settings, and registered package identity. A mismatch surfaces as [code]E_GS_NO_ACCESS[/code] ([code]0x80830002[/code]). The service configuration ID (SCID) is supplied automatically from the initialized Xbox services scaffold.
	</description>
	<tutorials></tutorials>
	<methods>
		<method name="get_folder_async">
			<return type="Signal" />
			<param index="0" name="user" type="GDKUser" />
			<description>
				Resolves the local, cloud-synced save folder for [param user] via [code]XGameSaveFilesGetFolderWithUiAsync[/code]. Returns a [Signal] that completes with a [GDKResult] whose [code]data.path[/code] holds the absolute folder path. The platform may show a brief sync UI. Requires a signed-in [GDKUser] and an initialized runtime.
			</description>
		</method>
		<method name="get_remaining_quota_async">
			<return type="Signal" />
			<param index="0" name="user" type="GDKUser" />
			<description>
				Returns the remaining game-save storage quota for [param user] via [code]XGameSaveFilesGetRemainingQuota[/code]. Returns a [Signal] that completes with a [GDKResult] whose [code]data.bytes[/code] holds the remaining byte count. The underlying GDK entry point is synchronous and rejects calls made from a time-sensitive thread with [code]E_GS_ASYNC_FUNCTION_REQUIRED[/code] ([code]0x8083000E[/code]), so the wrapper runs it on the shared task queue's work port and reports back on the main thread. Requires a signed-in [GDKUser] and an initialized runtime.
			</description>
		</method>
	</methods>
	<members>
	</members>
	<signals>
	</signals>
	<constants>
	</constants>
</class>
